import { Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle } from '../../../../.storybook/components';
import { ValidationNotification } from '../ValidationNotification';

<Title
  title="ValidationNotification"
  subtitle="Can be displayed anywhere on screen to inform the user of an action they just performed."
/>

<SectionTitle>Status</SectionTitle>

Change the status of the validation in order to inform the user of their actions result by modifying the `validationStatus` prop.

<Preview>
  <Story id="components-validationnotification--simple"/>
</Preview>

<SectionTitle>ValidationNotification Props</SectionTitle>
<Props of={ ValidationNotification } />
